package com.ibm.consulting.sim.proposal.application;

/** AI narration of an already-persisted deterministic decision snapshot. */
public record ProposalDecisionExplanationResponse(String message) {}
