# Docker Engine 29 requires API 1.44 or newer. Pin the test client so
# Testcontainers cannot negotiate the removed legacy API and silently skip the
# PostgreSQL correctness suites on current Docker Desktop installations.
api.version=1.44
