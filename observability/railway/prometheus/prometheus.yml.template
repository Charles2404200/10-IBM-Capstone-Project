global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: consulting-simulation-api
    metrics_path: /actuator/prometheus
    authorization:
      type: Bearer
      credentials: "${OBSERVABILITY_TOKEN}"
    static_configs:
      - targets: ["${API_METRICS_TARGET}"]
