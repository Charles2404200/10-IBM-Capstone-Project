global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: consulting-simulation-api
    metrics_path: /actuator/prometheus
    authorization:
      type: Bearer
      credentials: local-observability
    static_configs:
      - targets: ["api:8080"]